<h1><%= @song.name %></h1>
artist:
<%= link_to Artist.find(@song.artist_id).name, artist_path(Artist.find(@song.artist_id)) %><br>
genre:
<%= link_to Genre.find(@song.genre_id).name, genre_path(Genre.find(@song.genre_id)) %>